# GitHub Packages Registry Configuration
# Replace @YOUR_USERNAME or @YOUR_ORG with your actual GitHub username or organization

@YOUR_USERNAME:registry=https://npm.pkg.github.com
# Or for organization:
# @YOUR_ORG:registry=https://npm.pkg.github.com

# Registry for all other packages (default npm registry)
registry=https://registry.npmjs.org/

# Authentication token (don't commit this - use environment variable instead)
# //npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}

